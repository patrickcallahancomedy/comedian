name: DRIVE v0.14 Connector Micro-Pass

on:
  push:
    branches:
      - drive-playtest-v0.14
    paths:
      - "godot/**"
      - ".github/workflows/drive-v014-web.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: comedian-drive-v014
  cancel-in-progress: true

env:
  GODOT_VERSION: 4.7.1

jobs:
  test-and-export:
    runs-on: ubuntu-24.04
    container:
      image: barichello/godot-ci:4.7.1

    steps:
      - name: Checkout v0.14
        uses: actions/checkout@v4

      - name: Import Godot project
        shell: bash
        run: |
          set -euo pipefail
          godot --headless --editor --path godot --quit

      - name: Continuous topology test
        shell: bash
        run: |
          set -euo pipefail
          timeout 45s godot --headless --path godot --script res://tests/car_drive_test.gd

      - name: 100-trip drive playtest
        shell: bash
        run: |
          set -euo pipefail
          timeout 90s godot --headless --path godot --script res://tests/drive_playtest.gd

      - name: Drive regression suite
        shell: bash
        run: |
          set -euo pipefail
          timeout 45s godot --headless --path godot --script res://tests/drive_regression.gd

      - name: Prepare export templates
        shell: bash
        run: |
          mkdir -p "$HOME/.local/share/godot/export_templates"
          cp -a "/root/.local/share/godot/export_templates/${GODOT_VERSION}.stable" "$HOME/.local/share/godot/export_templates/"

      - name: Export web build
        shell: bash
        working-directory: godot
        run: |
          set -euo pipefail
          rm -rf build/drive-v014
          mkdir -p build/drive-v014
          godot --headless --path . --export-release "Web" build/drive-v014/index.html
          touch build/drive-v014/.nojekyll

          BUILD_NAME="drive-v014-${GITHUB_SHA:0:8}"
          mv build/drive-v014/index.js "build/drive-v014/${BUILD_NAME}.js"
          mv build/drive-v014/index.wasm "build/drive-v014/${BUILD_NAME}.wasm"
          mv build/drive-v014/index.pck "build/drive-v014/${BUILD_NAME}.pck"

          sed -i "s#src=\"index.js\"#src=\"${BUILD_NAME}.js\"#" build/drive-v014/index.html
          sed -i "s#\"executable\":\"index\"#\"executable\":\"${BUILD_NAME}\"#" build/drive-v014/index.html
          sed -i "s#\"index.pck\":#\"${BUILD_NAME}.pck\":#" build/drive-v014/index.html
          sed -i "s#\"index.wasm\":#\"${BUILD_NAME}.wasm\":#" build/drive-v014/index.html

      - name: Publish v0.14 connector micro-pass
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: godot/build/drive-v014
          target-folder: drive-v014
          clean: false
